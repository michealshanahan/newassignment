const sampleArray = [0, 5, 54, 67, 87, 5443, 5]
const nameArray = ["mike", "steve", "johnSON","sven"]
const doubleSample = sampleArray.map(function(num){
    return (num * 2)
})
console.log(doubleSample);

const strings = sampleArray.map(String);
console.log(strings)

function capitalizeNames(arr){
    const lowerArray = 
  }
  console.log(capitalizeNames(["john", "JACOB", "jinGleHeimer", "schmidt"])); 
